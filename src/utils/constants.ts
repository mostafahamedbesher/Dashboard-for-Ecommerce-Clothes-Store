export const sizesSchema = ["sm", "m", "l", "xl", "2xl"];

export const emailPattern = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;

export const productsNumItemsPerPage = 10;

export const ordersNumItemsPerPage = 10;
